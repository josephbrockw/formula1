Read docs/BACKLOG.md and present a summary of all items grouped by their `##` category.

Then ask which items I'd like to move to TASKS.md.

Before moving:
- Check the current count of items in TASKS.md.
- If adding the selected items would exceed 7, warn me and ask which existing tasks to /project:done or /project:park first.

When moving an item:
1. Add it to TASKS.md as a `- [ ]` entry.
2. Remove it from docs/BACKLOG.md.
