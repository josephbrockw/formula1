Add the following task to TASKS.md as a new `- [ ]` item:

$ARGUMENTS

Before adding, check the current number of items in TASKS.md. If there are already 7 items, do not add the task. Instead, tell me the list is full and suggest I either complete or /project:park an existing item first.
