Add the following item to docs/BACKLOG.md:

$ARGUMENTS

Instructions:
1. Parse the input for a category (Features, Tech Debt, Ideas, or Bugs) and description. If the input doesn't clearly specify a category, ask which one to use.
2. Open docs/BACKLOG.md and find the matching `##` heading.
3. Append the item as a `- ` entry under that heading (or under an existing `###` subcategory if one is specified or clearly fits).
4. Do not read or interpret other items in the file. Just locate the heading and append.
5. Do not create new `##` categories. If the requested category doesn't exist, list the available ones and ask.
6. You may create a new `###` subcategory under an existing `##` if I specify one that doesn't exist yet.
