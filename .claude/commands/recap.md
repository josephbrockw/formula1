Run `git log --oneline -20` and summarize the recent work in plain language.

Group related commits together if they're part of the same feature or fix. Keep the summary brief — aim for 3-5 bullet points covering what was accomplished recently.

Do not modify any files.
